项目背景与核心目标你是一个资深的 AI for Science/OR 研究员和 PyTorch 深度学习工程师。我现在有一个基于图神经网络（GNN）求解混合整数线性规划（MILP）的开源项目代码，来源于 ICLR 2023 论文："A GNN-GUIDED PREDICT-AND-SEARCH FRAMEWORK FOR MIXED-INTEGER LINEAR PROGRAMMING" (https://github.com/sribdcn/Predict-and-Search_MILP_method/)。当前状态：我已经准备好了代码库以及 Combinatorial Auction (CA) 问题的 MILP instances。核心目标：我希望你帮我完成以下整个工作流：使用 Gurobi 基于现有的 CA instances 生成训练所需的数据（二分图特征、标签即 Marginal Probabilities）。训练并评估 Baseline 模型（原仓库提供的 GNN 模型）。实现我对 GNN 输入特征编码层的创新改进（结合 DeepFM 和 AutoDis 的思想）。训练并评估改进后的模型，对比两者的 Loss 收敛情况以及在 CA 测试集上的 Primal Gap 性能。核心改进思路详述（Feature Engineering & Embedding）原论文在处理节点特征时，直接将所有特征拼接成一个一维向量输入给一个简单的 MLP 得到节点的初始 Embedding（维度通常为 64）。我希望引入推荐系统中处理异构特征的经典方法来替换这个初始输入层。1. 特征拆分与定义（参考论文 Appendix E）MILP 会被建模成一个二分图（Bipartite Graph），包含变量节点（Variable Nodes）和约束节点（Constraint Nodes）。我们需要将它们的特征拆分为连续和离散两部分。变量节点 (Variable Nodes):连续特征 (Continuous): 例如 obj (目标函数系数), v_coeff (平均约束系数), max_coeff, min_coeff, 变量上下界等（如果代码中有）。离散特征 (Discrete): 例如 int (是否为整数类型 0/1)。约束节点 (Constraint Nodes):连续特征 (Continuous): 例如 c_coeff (平均系数), rhs (右端项)。离散特征 (Discrete): 例如 sense (约束符号类别，如 <=, =, >=，需编码为类别索引)。2. 改进的模型架构 (DeepFM + AutoDis 思想)请你编写一个新的模块 EnhancedNodeEncoder，取代原模型中最初的特征映射层（Embedding 模块）。该模块的工作逻辑如下：离散特征处理 (Discrete Embedding):不直接使用 0/1 的数值，而是为每个离散特征维护一个独立的 nn.Embedding 查表，将其映射为一个固定维度（例如 embed_dim=16）的稠密向量。连续特征处理 (AutoDis 变体 / Value Scaling):不直接作为标量输入。为每个连续特征学习一个共享的基础向量（Base Embedding, 维度与离散特征相同 embed_dim=16）。然后，将输入的归一化连续特征值作为缩放系数（Scale），乘以这个 Base Embedding，得到该连续特征最终的稠密向量（即 $E_{final} = value \times E_{base}$）。特征交互 (DeepFM 思想):将上述所有转化成相同维度 (embed_dim) 的特征向量，在特征维度进行拼接（Concatenate）。然后将拼接后的向量展平（Flatten），送入一个多层感知机（MLP，可包含 LayerNorm 和 ReLU）。输出:该 MLP 的输出即为该节点的初始 Embedding（例如维度为 64），后续直接送入原模型的二分图卷积（Bipartite GCN/Half-convolution）网络进行消息传递，后续网络结构保持完全不变。（注：你需要分别为变量节点和约束节点实例化这样的 EnhancedNodeEncoder，因为它们的特征构成不同。）待执行任务与步骤分解 (Step-by-Step)请你一步一步执行，每完成一步请与我确认，然后再进行下一步。Step 1: 跑通数据生成流程检查代码库中用于生成训练数据的脚本（通常是收集 Gurobi 的求解结果并提取 bipartite graph 和 marginal probabilities）。编写或修改执行脚本，针对我提供的 CA instances 文件夹，调用 Gurobi 求解并生成 PyTorch Geometric 或自定义的图数据格式。确认生成的数据集中包含了我们需要的连续和离散特征。Step 2: 训练 Baseline 模型阅读原仓库的 GNN 模型代码（关注 model.py 或类似文件）。编写/梳理训练脚本 train_baseline.py，使用刚才生成的 CA 数据集进行训练。记录训练集的 Loss 曲线，以及在验证集上的表现，保存 Baseline 权重。Step 3: 实现改进版模型 (EnhancedNodeEncoder)新建一个文件或在原模型文件中扩展，编写 EnhancedNodeEncoder 类。你需要仔细阅读原模型输入数据的 tensor 结构，将原本合并在一起的特征在代码层面进行切片（Slicing），剥离出离散和连续特征。构建一个新的模型类（如 ImprovedPredictSearchModel），继承或修改原模型，将变量节点和约束节点的初始特征编码层替换为 EnhancedNodeEncoder。确保前向传播输出维度与 Baseline 原有的 GCN 层输入完美对接。Step 4: 训练改进模型并进行对比评估编写/梳理训练脚本 train_improved.py。在相同的 CA 数据集、相同的超参数（如 Batch Size, Learning Rate, Optimizer 等）下训练改进后的模型。输出对比报告：绘制 Baseline 和 Improved 模型的 Training Loss 对比图。最终验证：如果原代码包含 Search（Trust Region/局部搜索）的评估代码，请分别加载两个模型的权重，在 CA 的 Test Instances 上运行，对比两者的 Primal Gap 和求解时间。